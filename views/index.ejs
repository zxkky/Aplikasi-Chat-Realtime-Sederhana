<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat App</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        .chat-file {
            max-width: 300px;
            max-height: 300px;
            object-fit: cover;
            border: 2px solid #ccc;
            border-radius: 8px;
            transition: transform 0.3s ease;
            cursor: pointer;
        }

        .chat-file.full-size {
            max-width: 100%;
            max-height: 100%;
            object-fit: contain;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            margin: auto;
            z-index: 1000;
            background: rgba(0, 0, 0, 0.9);
        }
    </style>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="container mx-auto p-4 bg-white shadow-md rounded-lg">
        <h1 class="text-2xl font-bold mb-4">Chat</h1>
        <div id="mainbox" class="mb-4">
            <button id="createroomviewbtn" class="bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded">Create Room</button>
            <button id="joinroomviewbtn" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded">Join Room</button>
        </div>
        <div id="loginbox" class="mb-4" style="display: none;">
            <input type="text" id="username" placeholder="Enter your username" class="px-4 py-2 border border-gray-300 rounded focus:outline-none focus:border-blue-500 mb-2" />
            <input type="text" id="roomcode" placeholder="Enter room code" class="px-4 py-2 border border-gray-300 rounded focus:outline-none focus:border-blue-500 mb-2" />
            <button id="loginbtn" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded">Join Chat</button>
            <button id="backtomainbtn" class="bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded">Back</button>
        </div>
        <div id="createroombox" class="mb-4" style="display: none;">
            <input type="text" id="username_create" placeholder="Enter your username" class="px-4 py-2 border border-gray-300 rounded focus:outline-none focus:border-blue-500 mb-2" />
            <button id="createroombtn" class="bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded">Create Room</button>
            <button id="backtomainbtn_create" class="bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded">Back</button>
        </div>
        <div id="chatbox" style="display: none;">
            <div id="messages" class="max-h-96 overflow-y-auto border border-gray-300 rounded-lg p-4 mb-4"></div>
            <form id="form" class="flex mb-4">
                <input id="input" autocomplete="off" class="flex-grow px-4 py-2 border border-gray-300 rounded-l-lg focus:outline-none focus:border-blue-500" placeholder="Type your message..." />
                <button class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-r-lg">Send</button>
            </form>
            <form id="uploadForm" enctype="multipart/form-data" class="flex">
                <input type="file" id="fileInput" name="file" accept="image/*" class="px-4 py-2 border border-gray-300 rounded-l-lg focus:outline-none focus:border-blue-500" />
                <button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-r-lg">Upload File</button>
            </form>
        </div>
    </div>
    <div id="alertBox" class="hidden fixed top-4 left-1/2 transform -translate-x-1/2 bg-green-500 text-white text-sm px-4 py-2 rounded shadow-lg flex items-center space-x-2">
        <span>Room created with code:</span>
        <span id="roomCodeText" class="font-bold"></span>
        <button id="copyCodeBtn" class="bg-white text-green-500 px-2 py-1 text-xs rounded hover:bg-gray-100">
            Copy
        </button>
    </div>
    
    
    <script src="/socket.io/socket.io.js"></script>
    <script src="/client.js"></script>
</body>
</html>
